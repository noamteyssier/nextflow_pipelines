//include the following configs to avoid repetition

includeConfig '$baseDir/keepTraceReport.config'


//set parameters

params {
    reads_dir = "$launchDir/reads"
    out_dir = "$launchDir/results/"
    ID_file = "$launchDir/primerIDs.tsv"
    overlap_file = "$launchDir/primerOverlaps.tsv"
    }
